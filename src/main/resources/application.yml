spring:
  datasource:
    url: jdbc:mysql://db-cheap9.cmlb6weeeogc.ap-northeast-2.rds.amazonaws.com:3306/cheap9?serverTimezone=UTC
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    include: secret

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        # show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace #스프링 부트 2.x, hibernate5
# org.hibernate.orm.jdbc.bind: trace #스프링 부트 3.x,

